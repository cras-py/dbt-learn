sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    config:
          freshness: 
            error_after:
              count: 3
              period: day
            warn_after:
              count: 12
              period: hour
          loaded_at_field: _etl_loaded_at
    tables:
      - name: customers
        freshness: 
          null
        
      - name: orders

      - name: customers_copy